#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, q , size;
    cin >> n >> q;
    int** arr = new int*[n]; // to stroe value of sequence
    int** quer = new int*[q]; // to store the value of queries
    for (int i = 0; i < n; i++)
    {
        cin>>size;
        arr[i] = new int[size] ;
    
    for (int j = 0; j < size; j++)
    
        cin>>arr[i][j];
    }

    for(int i = 0 ; i<n-1 ; i++){
        
    }

    return 0;
}